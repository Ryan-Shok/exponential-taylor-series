MATLAB function that compares the internal exp(x) function with a written taylor series expansion. 
This expansion combines different signed terms separately for negative inputs to avoid rounding errors
from cancellations.
